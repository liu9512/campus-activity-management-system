# 后端数据库实验分支（未采用）

本分支尝试把 V1.0 的数据层迁移为前后端分离结构，由服务端 API 负责登录、活动查询、报名和取消。

未并入 `main` 的原因：

- 实验提交要求一个能够直接运行的初始版本；
- 当前开发环境没有可长期部署的后端服务和数据库；
- 增加服务器依赖会提高演示和复现成本；
- 最终版本改为浏览器端同步校验和 `localStorage`，并对单机数据边界作出说明。

该分支保留用于说明设计探索过程，不代表最终提交版本。
